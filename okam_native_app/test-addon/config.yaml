name: O-KAM HA Proxy (test)
version: 1.2.0-test1
slug: okam_ha_proxy_test
description: Temporary local add-on for testing a candidate build alongside the installed one
url: https://github.com/dunlabx/okam-ha-proxy
arch:
  - aarch64
  - amd64
startup: application
boot: manual
watchdog: http://[HOST]:[PORT:8099]/health
ports:
  8099/tcp: 8098
  8100/tcp: 8101
ports_description:
  8099/tcp: Native runtime status API (test instance)
  8100/tcp: RTSP camera streaming (test instance)
options:
  account_username: null
  account_password: null
  api_token: null
  cameras: []
  api_port: 8099
  rtsp_port: 8100
  idle_timeout_seconds: 120
  run_connect_test: false
  run_auth_test: false
  run_stream_test: false
  run_snapshot_test: false
  debug_credentials: false
schema:
  account_username: email
  account_password: password
  camera_password: password?
  api_token: password
  cameras:
    - uid: str
      alias: "str?"
      password: password?
      auth_mode: "match(automatic|configured_password)"
  api_port: "int(1,65535)"
  rtsp_port: "int(1,65535)"
  idle_timeout_seconds: "int(10,600)"
  run_connect_test: bool
  run_auth_test: bool
  run_stream_test: bool
  run_snapshot_test: bool
  debug_credentials: bool
